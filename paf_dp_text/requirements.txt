# PaF-DP-Text — requirements.txt
# =================================
# Pinned for Google Colab T4 (15 GB VRAM, Ubuntu 22.04).
#
# KEY NOTE: sentence-transformers is PINNED to 2.7.0
#   ≥3.x  → pulls torchcodec → needs libavutil.so (FFmpeg) → CRASH on Colab
#   2.2.2 → uses deprecated huggingface_hub.cached_download → CRASH
#   2.7.0 → stable, text-only, no media codec dependencies ✓
#
# torch is NOT listed here because Colab ships a CUDA build.
# Outside Colab:  pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu121

numpy==2.2.4
pandas==2.2.3
scipy==1.15.2
scikit-learn==1.6.1
tqdm==4.67.1
huggingface_hub==0.35.3
transformers==4.57.0
accelerate==1.5.2
datasets==3.4.1
sentence-transformers==2.7.0
mauve-text==0.4.0
gdown==5.2.0
matplotlib==3.9.0
seaborn==0.13.2
wordcloud==1.9.3
